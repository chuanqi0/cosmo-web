{% extends 'base.cbwa.html.twig' %}

{% block title %}全国婚礼作品设计大赛{% endblock %}

{% block body %}
    <div class="fe-register" ng-controller="RegisterController">
        <div class="container register">
            <div class="row fe-register-title">
                找回密码
            </div>
            <div class="row fe-register-description">
                本网站和时尚新娘App的账号是<strong>通用</strong>的哦
            </div>
            <div class="row fe-form">
                <div class="col-xs-6 left">
                    <div class="fe-form-top"><span class="fe-form-title">手机号</span></div>
                    <div class="fe-form-bottom">
                        <input type="text" placeholder="在这里输入手机号" ng-model="telephone" ng-class="{'fe-error': telephoneValid == false}" />
                    </div>
                </div>
                <div class="col-xs-6 right">
                    <div class="fe-form-top"><span class="fe-form-title">验证码</span></div>
                    <div class="fe-form-bottom">
                        <div class="col-xs-9 fe-p0">
                            <input type="text" placeholder="接收到的验证码" ng-model="code" ng-class="{'fe-error': codeValid == false}" />
                        </div>
                        <div class="col-xs-3 fe-p0 fe-pl15">
                            <div id="sendTimer" class="fe-btn code" ng-click="sendCode(2)">
                                发送
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-xs-6 left">
                    <div class="fe-form-top"><span class="fe-form-title">登录密码</span></div>
                    <div class="fe-form-bottom">
                        <input type="password" placeholder="在这里输入登录密码" ng-model="password" ng-class="{'fe-error': passwordValid == false}" />
                    </div>
                </div>
                <div class="col-xs-6 right">
                    <div class="fe-form-top"><span class="fe-form-title">确认密码</span></div>
                    <div class="fe-form-bottom">
                        <input type="password" placeholder="在这里重复输入登录密码" ng-model="rePassword" ng-class="{'fe-error': rePasswordValid == false}" />
                    </div>
                </div>
                <div class="col-xs-2 col-xs-offset-5 fe-p0 middle fe-pt20">
                    <div class="fe-btn next" ng-click="reset()">重置密码</div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script type="text/javascript" src="{{ asset('js/controller/register.js') }}"></script>
{% endblock %}